{
  "name": "synx-benchmarks",
  "version": "3.6.0",
  "private": true,
  "description": "SYNX format benchmarks — JSON vs YAML vs SYNX (JS/Native)",
  "scripts": {
    "bench:node":   "node --expose-gc bench_node.js",
    "bench:python": "python bench_python.py",
    "bench:rust":   "cd rust && cargo bench",
    "bench:all":    "npm run bench:node && npm run bench:python"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0",
    "js-yaml": "^4.1.1"
  }
}
